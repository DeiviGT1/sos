version: '3.8'

services:
  frontend:
    image: frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
  backend:
    image: backend
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports: 
      - "3001:3001"
